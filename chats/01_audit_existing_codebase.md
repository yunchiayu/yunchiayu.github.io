# Chat Prompt: Audit Existing Codebase

We are working in:

```text
/Users/yunchiayu/MyGitHub/yunchiayu.github.io
```

This is an existing codebase for a Jekyll-based academic website built on `al-folio`.

## Read First

- `AGENTS.md` if it exists
- `docs/ARCHITECTURE.md` if it exists
- `docs/AGENT_WORKFLOW.md` if it exists
- top-level tree and relevant source under the repository root
- prior reports under `reports/` when relevant

## Task

Audit the current codebase structure and write a report that explains:

- major directories and their roles,
- main entrypoints,
- important configs,
- local build and deployment commands,
- reference-only or generated directories,
- missing or stale documentation,
- immediate risks for future agents.

Write the report to:

```text
reports/<TASK_NAME>/01_Codebase_Audit.md
```

## Constraints

- Do not edit source code.
- Do not rewrite documentation yet.

## Stop Rule

After writing the audit, stop and wait for confirmation.
